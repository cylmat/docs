#####################################
### Resume of custom cmd for memo ###
#####################################

# useful cli urls
alias my='echo "
- URL -
https://designer.mocky.io
https://httpbin.org/legacy
https://dummyjson.com/users/1
https://jsonplaceholder.typicode.com/todos/1
https://hn.algolia.com/api/v1/search?query=react
https://data.opendatasoft.com/api/explore/v2.1/catalog/datasets

- COMMON -
busybox, nix, nu(shell), zsh

- DEV & DATA -
gron, iredis, jq, mnk (minikube), k9s, kdash, krew (ctx, ns)
sym(fony), tidy-viewer(csv), tig, tmux, tmuxinator, vim
>lazygit, lazydocker

- ENCRYPT
gnupg, openssl

- FILES & LOGS -
bat(cat), ccze, eza, fdf(ind), fzf, fzy, icdiff, 
lsof, rsync, tre(-command), ugrep, ranger, vifm

- HELPER & AI -
ai, cht(.sh)

- MONITOR & INFO -
btop, duf, dust, fastfetch, lshw, ncdu, procs, wtfutil, ~/welcome.sh
>procps (free, hugetop, kill, pgrep, pkill, pmap, ps, pwdx, skill
slabtop, snice, sysctl, tload, top, uptime, vmstat, w, watch)

- NETWORK -
dig, httpie, netstat, nmap, tcpdump, ss

- TRAIN -
ttyper, vimgolf

- UTILS & WORK -
uuidgen

- ZSH -
als, C-o (cmd), cph, cpf, z, zsh-bench
"'


### Other tools ###

# COMMON
# busybox, brew, nix, nu(shell), snap, zsh

# FILES & LOGS -
# autojump, ack, diff-so-fancy, delta, joshuto
# fpp, lnav, peco, sd, r(ip)g(rep)

# FUN
# cmatrix, cowsay, figlet, fortune, lolcat, nms, sl

# HELPER & AI -
# chat, explain(.sh), grex, navi, so, tldr, up, yai

# NETWORK
# bandwhich, curlie, http-prompt, lynx, w3m

# TRAIN
# gtypist, tt, ttyper, toipe, vimgolf

# UTILS & WORK
# bc, dnote, entr, glow, just, pass, scc
# taskwarrior, tokei, xdg-open, xsel

