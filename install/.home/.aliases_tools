#####################################
### Resume of custom cmd for memo ###
#####################################

# useful cli urls
alias my='echo "
- URL -
https://httpbin.org
http://headers.jsontest.com

- COMMON -
busybox, brew, nix, nu(shell), snap, zsh

- DEV & DATA -
gron, iredis, jq, mnk (minikube), k9s, kdash, krew (ctx, ns)
sym(fony), tidy-viewer(csv), tig, tmux, tmuxinator, vim
>lazygit, lazydocker

- ENCRYPT
gnupg, openssl

- FILES & LOGS -
ack, bat(cat), ccze, diff-so-fancy, delta, eza, fdf(ind), fzf
fzy, lsof, peco, rsync, sd, tre(-command), ugrep, r(ip)g(rep)
>joshuto, ranger, vifm

- FUN -
cmatrix, cowsay, figlet, fortune, lolcat, nms, sl

- HELPER & AI -
ai, chat, cht(.sh), explain(.sh)
grex, navi, so, tldr, up, yai

- MONITOR & INFO -
btop, duf, lshw, ncdu, neofetch, procs, ~/welcome.sh

- NETWORK -
bandwhich, curlie, httpie, http-prompt, lynx, w3m
netstat, nmap, tcpdump, ss

- TRAIN -
gtypist, tt, ttyper, toipe, vimgolf

- UTILS & WORK -
bc, dnote, entr, glow, just, pass, scc
taskwarrior, tokei, uuidgen, xdg-open, xsel
>procps (free, hugetop, kill, pgrep, pkill, pmap, ps, pwdx, skill
slabtop, snice, sysctl, tload, top, uptime, vmstat, w, watch)

- ZSH -
als, C-o (cmd), cph, cpf, z, zsh-bench
"'
